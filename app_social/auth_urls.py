# from django.urls import path
# from .auth_views import CustomTokenLoginView

# urlpatterns = [
#     path('api1/token/', CustomTokenLoginView.as_view(), name='api1-token-login'),
# ]
# from .views import UserFileAccessView

# urlpatterns += [
#     path('api2/files/', UserFileAccessView.as_view(), name='api2-user-files'),
# ]
